Below are the revised documentation recommendations:
{{input}}

Review these recommendations against the architectural document highlevel.md. For each recommendation:

1. Quote relevant sections from highlevel.md that either:
   - Support/agree with the recommendation
   - Conflict with the recommendation
   
2. For any conflicts found:
   - Draft specific changes needed in highlevel.md
   - Format the changes as a git patch
   
Present the git patch in standard format:
```patch
diff --git a/highlevel.md b/highlevel.md
index <hash>..<hash>
--- a/highlevel.md
+++ b/highlevel.md
@@ -<line>,<line> +<line>,<line> @@
< removed lines start with -
> added lines start with +
```
